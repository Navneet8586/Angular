version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
        - npm install
    build:
      commands:
        - ng build --prod
  artifacts:
    baseDirectory: dist/laundry
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
